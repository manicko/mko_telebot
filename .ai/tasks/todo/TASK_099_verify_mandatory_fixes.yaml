# TASK_099_verify_mandatory_fixes.yaml
id: TASK_099_verify_mandatory_fixes

title: Verify mandatory fixes for SPEC-DEVIATION issues

type: verification

status: pending

depends_on:
  - TASK_001_type_hints_monitor
  - TASK_002_exception_specificity_cli
  - TASK_003_apply_channel_defaults
  - TASK_004_secretstr_serialization

verifies:
  - TASK_001_type_hints_monitor
  - TASK_002_exception_specificity_cli
  - TASK_003_apply_channel_defaults
  - TASK_004_secretstr_serialization

verification_steps:
  - build: uv run ruff check src/mko_telebot
  - test: uv run pytest tests/ -v --tb=short
  - smoke_check: uv run mypy src/mko_telebot --ignore-missing-imports

pass_criteria:
  - ruff check passes with no errors
  - All tests pass
  - mypy passes with no new type errors

failure_action: return to rework
