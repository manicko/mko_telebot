---
id: task_007_remove_dead_code_launcher
title: Remove dead code launcher function from monitor.py
priority: medium
type: implementation
status: pending
depends_on: []

source_reference: .ai\audit\99-validation\01-cli-validated-findings.md
source_section: CLI-002

description: |
  The launcher() function (lines 343-352) is guarded by if __name__ == "__main__" 
  but is never used as an entry point. The pyproject.toml configures 
  "mko-telebot = mko_telebot.cli:app" as the CLI entry point. The run() command 
  in cli.py calls run_monitor() directly. No code imports or references launcher().

goals:
  - Remove unused launcher function
  - Remove redundant __name__ == "__main__" block
  - Reduce maintenance surface

files:
  - path: src/mko_telebot/monitor.py

    targets:
      - type: function
        name: launcher

    semantic_anchors:
      delete:
        type: function
        name: launcher

changes:
  - action: delete_symbol
    description: |
      Delete the launcher function (lines 343-352) and the adjacent 
      if __name__ == "__main__" block (line 355-356).

acceptance_criteria:
  - launcher function removed
  - No __name__ == "__main__" block remains
  - All tests pass