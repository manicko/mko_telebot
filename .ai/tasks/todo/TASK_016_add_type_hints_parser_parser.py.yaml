---
id: task_016_add_type_hints_parser
title: Add type hints to PatternParser private methods
priority: low
type: implementation
status: pending
depends_on: []

source_reference: .ai\audit\99-validation\08-quality-validated-findings.md
source_section: QLT-003

description: |
  The _peek() and _consume() methods in PatternParser lack return type hints.
  While these are private methods, adding type hints improves maintainability 
  and enables better IDE support per project standards.

goals:
  - Add type hints to _peek() method -> returns tuple[str, str] | None
  - Add type hints to _consume() method -> returns tuple[str, str] | None

files:
  - path: src/mko_telebot/core/parser.py

    targets:
      - type: method
        name: _peek
      - type: method
        name: _consume

    semantic_anchors:
      replace_signature:
        - type: method
          name: _peek
        - type: method
          name: _consume

changes:
  - action: replace_signature
    description: |
      Add parameter and return type hints:
      - def _peek(self): -> def _peek(self) -> tuple[str, str] | None
      - def _consume(self, expected_type=None): -> def _consume(self, expected_type: str | None = None) -> tuple[str, str] | None

acceptance_criteria:
  - Both methods have type hints
  - mypy passes without errors