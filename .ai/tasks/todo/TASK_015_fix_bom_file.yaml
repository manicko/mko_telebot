# TASK_015_fix_bom_file.yaml
# Advisory fix — UTF-8 BOM in core/__init__.py causes radon analysis failure

id: TASK_015_fix_bom_file

title: Remove UTF-8 BOM character from core/__init__.py

priority: low

depends_on: []

source_reference: .ai/audit/99-validation/09-structural-quality-validated-findings.md
source_section: STR-010
source_blocks:
  - "STR-010: UTF-8 BOM in File"

description: >
  The core/__init__.py file contains a UTF-8 BOM character (U+FEFF), causing
  radon analysis to fail with "invalid non-printable character" error. Remove
  the BOM byte sequence to ensure tools can parse the file correctly.

goals:
  - Remove UTF-8 BOM from core/__init__.py
  - Ensure radon can parse the file
  - No functional changes

files:
  - path: src/mko_telebot/core/__init__.py

changes:
  - action: remove_bom
    description: Remove UTF-8 BOM character if present
    code_hint: |
      # BOM is at start of file: \ufeff
      # Remove using sed or Python script
      # Alternatively, recreate file without BOM

acceptance_criteria:
  - No BOM character in file
  - radon can analyze the file
  - All imports work correctly