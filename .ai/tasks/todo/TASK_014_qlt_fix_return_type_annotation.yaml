id: TASK_014_qlt_fix_return_type_annotation

title: Add return type annotation to process_task function

priority: medium

status: pending

depends_on: []

description: >
  The process_task function in monitor_forward.py lacks a return type annotation.
  Add `-> None` to satisfy mypy --strict mode and follow project convention
  that all public functions have type hints.

goals:
  - Add -> None return type annotation to process_task
  - Satisfy mypy --strict requirement
  - Follow project type hint convention

files:
  - path: src/mko_telebot/monitor_forward.py

    targets:
      - type: function
        name: process_task

changes:
  - action: replace_signature
    description: >
      Add return type annotation to process_task signature

    code_hint: |
      async def process_task(task: Task, client: TelegramClient, settings: TelepostSettings) -> None:

acceptance_criteria:
  - mypy --strict passes without no-untyped-def error
  - ruff check passes
  - All tests pass