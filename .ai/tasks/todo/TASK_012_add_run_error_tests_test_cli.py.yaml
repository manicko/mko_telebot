---
id: task_012_add_run_error_tests
title: Add tests for run CLI command error paths
priority: medium
type: implementation
status: pending
depends_on:
  - task_008_add_exception_handling_run

source_reference: .ai\audit\99-validation\07-tests-validated-findings.md
source_section: TST-006

description: |
  The run CLI command (lines 86-100 in cli.py) exits with code 1 on config errors 
  but has no tests for error scenarios like missing config, Telegram client 
  initialization failure, or auth errors. The command is skipped entirely in tests.

goals:
  - Add test for run command with missing config (exit code 1)
  - Add test for run command with invalid config (exit code 1)
  - Follow existing patterns from test_cli.py validate tests

files:
  - path: tests/test_cli.py

    targets:
      - type: class
        name: TestCliRun

      - type: function
        name: test_run_missing_config
        module: test_cli.py

    semantic_anchors:
      insert_after:
        type: class
        value: TestCliValidate

changes:
  - action: add_top_level
    description: |
      Add TestCliRun class with error path tests following existing patterns.
      Tests needed:
      - test_run_missing_config_exits_1
      - test_run_invalid_secrets_exits_1

acceptance_criteria:
  - TestCliRun class exists in test_cli.py
  - Tests for error paths pass
  - Exit codes verified for error conditions