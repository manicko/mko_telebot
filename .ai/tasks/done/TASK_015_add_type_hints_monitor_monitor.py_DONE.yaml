---
id: task_015_add_type_hints_monitor
title: Add return type hints to monitor.py public functions
priority: medium
type: implementation
status: pending
depends_on:
  - task_007_remove_dead_code_launcher

source_reference: .ai\audit\99-validation\03-services-validated-findings.md
source_section: SRV-002

description: |
  Two public functions in monitor.py lack return type hints:
  - build_message_link (line 72) -> returns str | None
  - launcher (line 343) -> returns None (but is slated for removal)
  
  Only build_message_link requires fixing since launcher() is removed in task_007.

goals:
  - Add return type hint to build_message_link function
  - Follow project type hint standards
  - Pass mypy type checking

files:
  - path: src/mko_telebot/monitor.py

    targets:
      - type: function
        name: build_message_link

    semantic_anchors:
      replace_signature:
        - type: function
          name: build_message_link

changes:
  - action: replace_signature
    description: |
      Add return type hint to build_message_link.
      Change: def build_message_link(msg):
      To: def build_message_link(msg) -> str | None:

acceptance_criteria:
  - build_message_link has return type hint
  - mypy passes without errors
  - ruff passes