---
id: task_009_add_task_model_tests
title: Add unit tests for Task model state management
priority: high
type: implementation
status: pending
depends_on: []

source_reference: .ai\audit\99-validation\07-tests-validated-findings.md
source_section: TST-001

description: |
  The Task class implements critical async state management logic including load_state(), 
  save_state(), resolve_channel_entity(), resolve_targets_entities(), and set_offset_date(). 
  None of these methods have unit tests. A failure in state persistence could cause 
  message duplication or missed messages on restart.

goals:
  - Create tests/test_task.py with async test patterns
  - Test state file creation and loading
  - Test entity resolution error paths
  - Test offset date computation
  - Test error handling for file I/O failures

files:
  - path: tests/test_task.py

    targets: []

    semantic_anchors:
      file_level: true

changes:
  - action: add_top_level
    description: |
      Create new test file with mocked Telethon client and temp directories.
      Test cases needed:
      - test_task_load_state_missing_file
      - test_task_save_state_creates_file
      - test_task_resolve_channel_entity_error
      - test_task_set_offset_date_computation
      - test_task_state_io_error_handling

acceptance_criteria:
  - tests/test_task.py exists with >10 test cases
  - All tests pass
  - Coverage for state persistence methods increases