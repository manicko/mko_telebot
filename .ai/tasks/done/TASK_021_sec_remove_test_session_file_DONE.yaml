id: task_021_sec_remove_test_session_file

source_reference: .ai\audit\99-validation\04-security-validated-findings.md
source_section: SEC-002

title: Remove test.session file from repository root

priority: low

description: >
  SEC-002: `test.session` (28,672 bytes) exists at repository root as an accidental
  commit. It is ignored by .gitignore but should be removed from the repository.
  The session path guard already exists in code.

goals:
  - Remove test.session from repository
  - Clean up accidental artifact

files:
  - path: test.session
    targets:
      - type: file
    semantic_anchors:
      - action: delete_file

changes:
  - action: remove_artifact
    description: >
      Delete test.session from repository root. It is already gitignored so no future
      commits will include it.

acceptance_criteria:
  - File removed from repository
  - No functional change
  - .gitignore already prevents future commits

tests_to_run:
  - git status (verify file is removed and ignored)