id: TASK_008_fix_channels_model_config
title: Fix unannotated model_config in channels.py
priority: low

depends_on: []

source_reference: .ai\plans\IMPLEMENTATION_PLANS.md
source_section: Error 7

description: >
  Add ClassVar[ConfigDict] annotation for model_config in Pydantic model classes.
  ChannelConfig, ChannelDefaults, and ChannelsConfig all have unannotated model_config
  that needs ClassVar annotation.

goals:
  - Add ClassVar import
  - Annotate model_config in all three classes
  - Pass type checking

files:
  - path: src/mko_telebot/core/channels.py
    targets:
      - type: class
        name: ChannelConfig
      - type: class
        name: ChannelDefaults
      - type: class
        name: ChannelsConfig

changes:
  - action: add_import
    description: >
      Add `from typing import ClassVar` import at top of file

  - action: modify_field
    description: >
      Update model_config in ChannelConfig to:
      model_config: ClassVar[ConfigDict] = ConfigDict(extra="forbid")

  - action: modify_field
    description: >
      Update model_config in ChannelDefaults to:
      model_config: ClassVar[ConfigDict] = ConfigDict(extra="forbid")

  - action: modify_field
    description: >
      Update model_config in ChannelsConfig to:
      model_config: ClassVar[ConfigDict] = ConfigDict(extra="forbid")

acceptance_criteria:
  - basedpyright passes on channels.py (no model_config warnings)
  - ruff check passes on channels.py

tests_to_run:
  - uv run ruff check src/mko_telebot/core/channels.py
  - uv run basedpyright src/mko_telebot/core/channels.py

RESULT: COMPLETED
All model_config annotations fixed with ClassVar[ConfigDict].
Ruff: passed
basedpyright: 0 errors (11 pre-existing warnings unrelated to task)