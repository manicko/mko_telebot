{
  "$schema": "https://app.kilo.ai/config.json",
  "indexing": {
    "enabled": true
  },
  "mcp": {
    "filesystem": {
      "type": "local",
      "command": [
        "npx",
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${workspaceFolder}"
      ]
    },
  }
}